{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "cheer-choreo-dev",
      "runtimeExecutable": "/bin/bash",
      "runtimeArgs": [
        "-c",
        "export NVM_DIR=$HOME/.nvm && . $NVM_DIR/nvm.sh && npm run dev -- --port 3000"
      ],
      "port": 3000
    }
  ]
}
